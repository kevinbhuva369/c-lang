#include <stdio.h>
int main()
{
    int dijit, i = 1;
    printf("ener dijit:");
    scanf("%d", &dijit);

    for (dijit != 0; dijit /= 10; i++)
    {
    }
    printf("you enetr %d dijit numbar", i);
}